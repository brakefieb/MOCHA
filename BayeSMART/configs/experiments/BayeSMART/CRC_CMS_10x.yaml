method_name: BayeSMART
cohort_name: CRC_CMS_10x
overrides:
  runtime:
    level: cohort
    integration_scope: subgroup
  parameters:
    k: 6
    w: 0.1
    n_neighbor: 6
  preprocessing:
    gene_select: sparkx
    n_gene: 2000
    pcn: 3
notes:
- Integrate paired Rep1/Rep2 sections within each subject instead of running all
  14 sections together.
- Keep BayeSMART preprocessing and model settings consistent with the original method.
