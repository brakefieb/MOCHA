method_name: ResST
cohort_name: MOB_ST
overrides:
  runtime:
    integration_scope: subgroup
  parameters:
    n_domains: 5
    refine_shape: square
notes:
- Keep K aligned with the existing methods for this cohort.
