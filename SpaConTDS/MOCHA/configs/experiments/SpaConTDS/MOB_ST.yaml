method_name: SpaConTDS
cohort_name: MOB_ST
overrides:
  parameters:
    n_domains: 5
    k: 5
notes:
- Use the same K as the other MOCHA benchmark methods for MOB_ST.
- Keep SpaConTDS demo defaults unless explicitly overridden here.
