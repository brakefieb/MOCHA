method_name: SpaConTDS
cohort_name: DLPFC_10x
overrides:
  parameters:
    n_domains: 7
    k: 7
notes:
- Use the same K as the other MOCHA benchmark methods for DLPFC_10x.
- Keep SpaConTDS demo defaults unless explicitly overridden here.
