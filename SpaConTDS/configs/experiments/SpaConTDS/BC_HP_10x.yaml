method_name: SpaConTDS
cohort_name: BC_HP_10x
overrides:
  parameters:
    n_domains: 6
    k: 6
notes:
- Use the same K as the other MOCHA benchmark methods for BC_HP_10x.
- Keep SpaConTDS demo defaults unless explicitly overridden here.
