method_name: SpaConTDS
cohort_name: CRC_CMS_10x
overrides:
  parameters:
    n_domains: 6
    k: 6
notes:
- Use the same K as the other MOCHA benchmark methods for CRC_CMS_10x.
- Keep SpaConTDS demo defaults unless explicitly overridden here.
